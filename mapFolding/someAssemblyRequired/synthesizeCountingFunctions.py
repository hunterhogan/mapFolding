from mapFolding.someAssemblyRequired import Make
from typing import cast
from types import ModuleType
import ast

from mapFolding.someAssemblyRequired.transformationTools import IngredientsFunction

def Z0Z_makeCountingFunction(callableTarget: str, sourceAlgorithm: ModuleType, inline: bool, dataclass: bool):
	pass
