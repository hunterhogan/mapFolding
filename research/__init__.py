"""Access exploratory research code for map folding and meanders.

(AI generated docstring)

You can use this package to find exploratory computations, prototypes, and analysis code that
support the main `mapFolding` project. This package groups code-first investigations that may
move, split, or disappear as ideas mature, so `research` is more useful as a working area than as
a stable public API.

Organization
------------
This package favors executable experiments and computational analysis over prose-oriented project
materials. The contents of `research` change frequently as experiments are added, renamed,
archived, or promoted into more stable packages.

Stability
---------
Import paths under `research` should be treated as provisional. A result or technique that proves
useful may later move into a production package, while abandoned experiments may remain as
historical context.
"""
